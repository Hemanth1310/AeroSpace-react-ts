import React from 'react'

type Props = {}

const Header = (props: Props) => {
  return (
    <div className='w-screen bg-gray-950 text-blue-50 fixed p-4 text-2xl'>
        AeroSpace</div>
  )
}

export default Header